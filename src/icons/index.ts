/*
 * Nucleo UI icons (Design/Development set), outline · 18px — copied from the
 * local Nucleo install rather than pulled from a package, so there is no
 * dependency to track. Re-copy from ~/.nucleo/skills/ui/components to update.
 */
export { CodeBranchOutline18 } from './CodeBranchOutline18'
